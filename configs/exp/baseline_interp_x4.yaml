project:
  stage: 1

seed: 42

data:
  dataset_name: emnist
  emnist_split: letters
  split: train
  num_samples: 64
  val_ratio: 0.1
  hr_size: 96
  scale: 4
  root: outputs/cache/emnist

model:
  name: interpolation
  mode: bicubic

train:
  epochs: 0

eval:
  save_grid: true
  save_individual: true

runtime:
  download: false

output:
  dir: outputs/interpolation/train
