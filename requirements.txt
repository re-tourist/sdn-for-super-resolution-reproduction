# Python 3.10+ recommended.
# This file covers the current Stage 1/2 workflow:
# EMNIST data inspection, interpolation baseline, electronic baseline training, and evaluation.

numpy>=1.24,<2.0
matplotlib>=3.7,<3.10
Pillow>=10,<12
PyYAML>=6,<7
scikit-image>=0.22,<0.25

# PyTorch stack used by the current training / dataset pipeline.
# If the Linux machine has a specific CUDA version requirement, install the matching
# torch/torchvision wheels first, then run `pip install -r requirements.txt`.
torch==2.5.1
torchvision==0.20.1
